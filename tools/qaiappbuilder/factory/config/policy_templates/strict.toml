# Generated by tools/build/factory_compiler from config/policy_templates\strict.json.
# Static default — do not edit at runtime; edit the source file in the repo and re-run the migrator.

name = "strict"
label = "Strict (All operations require confirmation)"
description = "All file and command operations require explicit user approval. Best for high-security environments."
mode = "enforce"
dynamic_authorization = true

[auto_approve]
read = false
write = false
exec = false
