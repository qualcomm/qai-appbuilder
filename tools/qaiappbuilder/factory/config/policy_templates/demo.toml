# Generated by tools/build/factory_compiler from config/policy_templates\demo.json.
# Static default — do not edit at runtime; edit the source file in the repo and re-run the migrator.

name = "demo"
label = "Demo (Read-only, no dynamic authorization)"
description = "Read-only mode with no dynamic authorization popups. AI can only read files. Best for demos and presentations."
mode = "enforce"
dynamic_authorization = false

[auto_approve]
read = true
write = false
exec = false
