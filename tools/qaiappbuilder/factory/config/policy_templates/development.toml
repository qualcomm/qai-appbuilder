# Generated by tools/build/factory_compiler from config/policy_templates\development.json.
# Static default — do not edit at runtime; edit the source file in the repo and re-run the migrator.

name = "development"
label = "Development (Read auto-approve, write/exec require confirmation)"
description = "Project files can be read without confirmation. Write and execute operations still require approval. Good for active development."
mode = "enforce"
dynamic_authorization = true

[auto_approve]
read = true
write = false
exec = false
