# BERT tokenizer is handled by bert_tokenizer_local.py
# which reads binary files (bert_zh_tokenizer.bin + bert_normalizer.bin)
# from the models/melotts-zh/ directory at runtime.
# No text-based vocab.txt is needed.
