# Open-source release edition marker (auto-generated by the release
# pipeline). The internal-only feature modules are physically absent
# from this tree, so the frontend build defaults to the external
# feature set. Do not set this to `internal` here.
QAI_EDITION=external
